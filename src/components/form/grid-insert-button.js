import React from "react";

const GridInsertButton =({title })=>{
    const insertOnClickEvent =(event)=>
    {
        


    }

    return (

        <button  value={title} onClick={insertOnClickEvent} > </button>
    );

}

export default GridInsertButton ;